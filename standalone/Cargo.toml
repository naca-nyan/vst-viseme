[package]
name = "standalone"
version = "0.1.0"
edition = "2024"

[dependencies]
nih_plug = { git = "https://github.com/polartape/nih-plug.git", branch = "keyboard-fix", features = ["standalone"] }
vst_viseme = { path = ".." }
